{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        // The tests live outside src/ and import from ../src, so widen the root and
        // add the globals they need (node built-ins + the mocha describe/it API).
        "rootDir": "..",
        "types": ["@iobroker/types", "node", "mocha"]
    },
    "include": ["./**/*.ts"]
}
